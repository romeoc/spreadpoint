<?php $campaignHelper = $this->campaignHelper(); ?>
<?php $campaignHelper->setData($this->data); ?>
<section class="campaign-section-schedule">
    <div class="campaign-form-row custom-form-row custom-row-type">
        <div class="field">
            <label class="type">* Competition Type</label>
            <?php $first = true; ?>
            <?php foreach ($campaignHelper->getAllCompetitionTypes() as $type): ?>
                <div class="radio-element <?php echo $type['classes']; ?>">
                    <input id="<?php echo $type['id']; ?>" type="radio" name="type" value="<?php echo $type['value']; ?>" <?php echo $type['checked']; ?> />
                    <label class="click-note" for="<?php echo $type['id']; ?>" title="<?php echo $type['note']; ?>"><?php echo $type['title']; ?></label>
                </div>
            <?php endforeach; ?>
        </div>
    </div>
    <div class="campaign-form-row custom-form-row custom-row-startime">
        <div class="field">
            <label class="title">* Start Date</label><input class="jq-datetimepicker focus-note" type="text" name="startTime" value="<?php echo $campaignHelper->get('startTime'); ?>"/>
        </div>
    </div>
    <div class="campaign-form-row custom-form-row custom-row-endtime">
        <div class="field">
            <label class="title">* End Date</label><input class="jq-datetimepicker focus-note" type="text" name="endTime" value="<?php echo $campaignHelper->get('endTime'); ?>" />
        </div>
    </div>
    <div class="campaign-form-row custom-form-row custom-row-timezone">
        <div class="field">
            <label class="timezone">* Time Zone</label>
            <select name="timezone" class="right focus-note">
                <?php foreach($campaignHelper->getTimezones() as $timezone): ?>
                    <option value="<?php echo $timezone ?>" <?php if ($campaignHelper->get('timezone') == $timezone) { echo 'selected'; } ?>><?php echo $timezone ?></option>
                <?php endforeach; ?>
            </select>
        </div>
    </div>
    <div class="campaign-form-row custom-form-row custom-row-cycleDuration">
        <div class="field">
            <label class="cycleDuration">* Cycle Duration</label><input class="focus-note" name="cycleDuration" type="text" value="<?php echo $campaignHelper->get('cycleDuration'); ?>" title="In days" />
        </div>
    </div>
    <div class="campaign-form-row custom-form-row custom-row-cyclesCount">
        <div class="field">
            <label class="cyclesCount">* Cycles Count</label><input class="focus-note" name="cyclesCount" type="text" value="<?php echo $campaignHelper->get('cyclesCount'); ?>" title="Set 0 for an infinte number of cycles." />
        </div>
    </div>
</section>